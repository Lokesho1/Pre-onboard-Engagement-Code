import { Patient } from "./patient";

export class Doctor {
    constructor(
        id: number,
        name: String,
        specialized: String
        // patients:Array<Patient>
    ) { }

}